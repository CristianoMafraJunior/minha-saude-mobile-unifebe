import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.unifebe.minhasaude',
  appName: 'Minha Saude UNIFEBE',
  webDir: 'dist'
};

export default config;
